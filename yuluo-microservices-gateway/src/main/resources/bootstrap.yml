server:
  port: 9280

spring:

  # 应用名
  application:
    name: yuluo-microservices-gateway

  # 激活的配置文件
  profiles:
    active: dev

  # gateway 使用 reactive 实现，不兼容 mvc ，因此关闭
  main:
    web-application-type: reactive

#logging:
#  level:
#    root: debug
